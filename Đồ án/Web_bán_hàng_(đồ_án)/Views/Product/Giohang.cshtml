

@model IEnumerable<Web_bán_hàng__đồ_án_.Models.Product>

@{
    ViewBag.Title = "Giohang";
    Layout = "~/Views/Shared/_LayoutWeb.cshtml";


}

<!--  <link rel="stylesheet" href="~/wwwroot/css/styles.css" />
<script src="~/wwwroot/js/scripts.js"></script>/> -->

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giỏ Hàng</title>
    <style>
        .cart-container {
            width: 80%;
            margin: auto;
            border: 1px solid #ddd;
            padding: 20px;
        }

        .cart-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }

            .cart-item img {
                width: 100px;
            }

        .cart-item-info {
            flex: 1;
            padding: 0 20px;
        }

        .cart-total {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            font-size: 1.2em;
            border-top: 1px solid #ddd;
        }

        .checkout-button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #28a745;
            color: white;
            text-align: center;
            text-decoration: none;
            font-size: 1.2em;
            border-radius: 5px;
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <div class="cart-container">
        <h3 class="mb-4">Giỏ Hàng</h3>
        <div>
            @foreach (var item in Model)
            {
                <div class="cart-item row">
                    <div class="col-md-2">
                        <img src="@item.ProductImage" alt="@item.ProductName" class="product-image" />
                    </div>
                    <div class="col-md-4">
                        <h5>@item.ProductName</h5>

                    </div>
                    <div class="col-md-2">
                        <p>Đơn Giá: @item.ProductPrice.ToString("N0")đ</p>
                    </div>
                    <div class="col-md-2">
                        <p>Số Lượng: @item.StockQuantityPro</p>
                    </div>
                    <div class="col-md-2">
                        <p class="total">Thành Tiền: @item.ProductPrice.ToString("N0")đ</p>
                    </div>
                </div>
            }
        </div>
        <div class="mt-4 text-end">
            <a href="/Checkout" class="btn btn-primary">Mua Hàng</a>
        </div>
    </div>



</body>
</html>



